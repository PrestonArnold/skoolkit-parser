# @prestonarnold/skoolkit-parser

Transforms .skool files into a structured JSON model

## Usage
```ts
import { parseSkool } from "@prestonarnold/skoolkit-parser";

const result = parseSkool(rawSkool);
console.log(result)
```
